# 视汇 (VFusion) - 项目更新日志 (CHANGELOG)

## [v0.12.0] - 2026-08-11

### 🖼️ 上传图片预览等比自动缩放、暗色高斯模糊衬底、第三方 Webhook 对接规范与数据无损平滑升级 (Image Upload Auto-Scaling, Ambient Blur Backdrop, Webhook Spec & Seamless Upgrade Release v0.12.0)

#### 1. 🖼️ 上传抓拍图片预览区域等比自动缩放 (`object-fit: contain`) 与暗色毛玻璃背景
* **等比智能缩放**：重构视频网发布终端（Collector）抓拍照片上传预览容器，由原本的 `object-fit: cover` 升级为 **`object-fit: contain`** 等比缩放渲染，彻底消除了纵向/横向非标准比例照片被强制截断与边缘裁切的问题，100% 完整呈现场凭证细节。
* **高阶暗色毛玻璃衬底 (Ambient Blur Backdrop)**：为预览容器增加暗色背景 (`#0f172a`) 与高斯模糊 (`filter: blur(20px) brightness(0.45)`) 衬底，自动提取当前图片的配色调性填充缩放留白区域，打造高质感现代化影音级界面表现。
* **全域拖拽上传 (Drag & Drop) 与 Modal 弹窗放大**：集成全域拖拽上传事件监听 (`drag-over`)；优化底部浮层信息栏，支持点击图片或【🔍 放大】按钮唤起 Modal Lightbox 全屏磨砂弹窗放大查看大图凭证。

#### 2. ⚡ 内网接收中台架构精简与 Analytics 页签解耦
* **移除冗余 Analytics 图表页签**：移除了内网接收中台（Core）冗余的 `analytics` 静态统计图表页签及对应后端 API (`/api/analytics`)，精简前端资源并解耦代码逻辑。
* **9 大功能页签精粹对齐**：重构中台导航页签与模版异步加载器，提升页面响应流畅度。

#### 3. 📖 第三方应用系统双模式对接说明文档 (Webhook & REST API Integration)
* **Webhook 主动推送模式文档**：新增 [third_party_webhook_spec.md](file:///e:/vfusion/docs/third_party_webhook_spec.md)，详细阐述 HMAC-SHA256 摘要签名算法规则、请求头格式、重试与死信隔离机制，并附带 Node.js / Java / Python 多语言示例代码。
* **REST API 被动拉取模式文档**：新增 [third_party_integration_guide.md](file:///e:/vfusion/docs/third_party_integration_guide.md)，规范外发接口鉴权、游标增量拉取与自动解包规范。

#### 4. 🌐 第三方 FTP 传输管道高质感 3 列卡片重构与翡翠绿 Toggle 开关
* **3 大分组子卡片布局**：重构视频网与内网两端【FTP 通道配置】界面为【服务器连接参数】、【远程目录与前缀】、【自动清理与高阶选项】3 大高质感独立分组子卡片，适配全屏显示，消除长行挤压。
* **翡翠绿 Toggle Switch 开关**：将 FTP 通道状态与自动清理选项升级为高标准翡翠绿 (`#16a34a`) 现代滑动开关。
* **强行释放 Socket 机制**：增强 `ftp_client.js` 连接池异常捕获与 Socket 强行释放，解决轮询连接残留导致 `Server sent FIN packet` 的网络困扰。

#### 5. 🛡️ 生产环境 SQLite 数据库与账号平滑无损升级
* **数据库持久化保护**：彻底解决版本解压更新时可能导致生产环境 SQLite 数据库（`vfusion_core.db` / `vfusion_collector.db`）和账号配置被覆盖重置隐患，保证已有历史事件数据无损平滑迁移。
* **双格式扫描扩展**：修复异常隔离区扫描过滤 Bug，统一支持 `.zip` 与 `.jpg` 伪装格式单据数据包解析。

#### 6. 🧪 端到端 E2E 全流程测试自动化脚本 (`verify_e2e.js`)
* 自动化测试脚本覆盖鉴权、发布、HMAC 校验、隔离摆渡、SQLite 存储与 Webhook 转发全链路，**6/6 测试用例 100% 绿灯通过**。

---

## [v0.11.0] - 2026-08-11

### 🛡️ 视汇安全加固里程碑：HMAC 密钥轮换、Git 历史敏感数据深度清理、.env 环境变量支持与端口重构 (Security Hardening, Git History Purge, Zero-Dep .env Support & Port Refactoring Release v0.11.0)

#### 1. 🛡️ HMAC 密码学密钥轮换与安全合规
* **256 位强随机密钥生成**：使用 `crypto.randomBytes(32).toString('hex')` 重新生成强随机 HMAC 密钥与 Token 密钥并写入 `storage/security.json`。
* **默认泄露密钥强制淘汰**：服务端启动时增加防泄露检测，若发现硬编码默认密钥 `vfusion_secret_key_2026` 会自动将其强制替换为强随机密钥并打印警告提示。

#### 2. 🧹 Git 历史敏感文件深度清理与重写 (Git History Purge)
* **Git 历史重写**：使用 `git filter-branch` 彻底清洗仓库 44 个 Commit 历史，从过去的提交痕迹中抹除了 `storage/security.json`（已泄露 HMAC 密钥）、`users.json`（用户账号与哈希）、`.db`（SQLite 数据库）及真实事件现场照片文件。
* **解绑跟踪与垃圾回收**：解除了 Git 对 `storage/` 目录下除 `.gitkeep` 外的所有运行时文件跟踪，删除了历史备份引用，并执行 `git reflog expire` 和 `git gc --prune=now --aggressive` 深度垃圾回收，确保 Git 历史全量无敏感词残存。

#### 3. ⚙️ 零依赖 `.env` 环境变量配置文件支持 (`packages/common/env_loader.js`)
* **零依赖加载器**：新增轻量级环境变量加载模块 [env_loader.js](file:///e:/vfusion/packages/common/env_loader.js)，在视频网端与内网端服务启动时自动解析并注入根目录下的 `.env` 和 `.env.local` 文件。
* **模版与 Git 隔离**：根目录下增设包含详细注释的 [.env.example](file:///e:/vfusion/.env.example) 模版文件以及 [.env](file:///e:/vfusion/.env) 配置文件，并在 [.gitignore](file:///e:/vfusion/.gitignore) 中加入了 `.env` 防泄露隔离规则。

#### 4. 🐛 内网登录输入框自动清空 Bug 彻底修复
* **未登录轮询保护**：修复了在未登录状态下 `loadAlerts()` 和 `loadFtpPollStatus()` 定时轮询被触发、服务端返回 401 响应触发 `window.location.reload()` 导致页面每 5~6 秒被自动刷新擦除用户名输入框的问题。
* **拦截逻辑优化**：优化 `main.js` 中的 `apiFetch` 通用拦截器，移除了强制 `location.reload()`，未登录状态下不干扰用户正常的表单输入过程。

#### 5. 🔌 默认服务端口重构 (Port Refactoring to 5001 & 5002)
* **视频网发布终端 (Collector)**：默认监听端口由 `4001` 修改为 **`5001`**（支持 `COLLECTOR_PORT` 或 `PORT` 环境变量覆盖）。
* **内网数据中台 (Core)**：默认监听端口由 `4002` 修改为 **`5002`**（支持 `CORE_PORT` 或 `PORT` 环境变量覆盖）。
* **全链路脚本与文档联动**：一键启动脚本（`start_all.bat` / `start_all.sh`）、独立启动脚本、健康诊断、E2E 测试脚本及 README 文档同步更新端口配置。

---

## [v0.10.0] - 2026-08-11

### 🚀 视汇核心里程碑：第三方外部 FTP 服务器双端自动传输管道与独立可视化控制台 (Third-Party FTP Automation Pipeline & Standalone Console Release v0.10.0)

#### 1. 🌐 视频网端与内网端【FTP 通道配置】双端独立一级页面部署
* **解耦提升为独立页签**：在视频网发布终端（Collector）与内网接收中台（Core）的顶部导航栏中，同步将【FTP 通道配置】拆分并提升为独立的一级 Tab 页签（`tabBtn-ftp`）。
* **全参数专属可视化表单**：在内网数据中台与视频网终端中集成【第三方 FTP 服务器通道可视化配置卡片】，支持管理员在线配置：
  * FTP 远程服务启用开关（`ftp_enabled`: 开/关）
  * 第三方 FTP 服务器 IP / 域名（`ftp_host`）
  * 服务端口号（`ftp_port`: 默认 21）
  * 登录账号与鉴权密码（`ftp_user`, `ftp_password`）
  * 远程 FTP 专用接收/发送目录（`ftp_remote_dir`: 例如 `/vfusion_packages`）
  * 专属单据包识别前缀过滤（`pkg_prefix`: 默认 `vfusion_`）
  * 内网抓取入库后自动清理远程文件（`ftp_delete_after_download`: 开/关）
* **一键在线测试 FTP 连通性**：提供 `[在线测试 FTP 连通性]` 按钮与后端接口 (`POST /api/config/ftp/test`)，在线校验 FTP IP、端口、账号密码鉴权及远程目录读写权限，并提供实时色块状态诊断反馈。

#### 2. 🎨 UI 布局与 Typography 统一排版规范重构
* **规范卡片报头 (`.card-header`)**：彻底消除了 `ftp.html` 中零散的内联字体大小及颜色覆盖样式，继承平台统一的 `--font-family` 及 `--text-main` 设计 Token；操作按钮全部右对齐收纳，与平台其他页面（如 `publish.html`、`webhooks.html`）保持 100% 视觉对齐。
* **双列流式表单 (`.form-grid`)**：使用全局 `.form-grid`（2 列响应式布局）替代单行网格，使 IP、端口、密码等输入框获得适宜展示宽度与安全间距，彻底统一全局 `label` 与 `input` 样式规范。

#### 3. ⚙️ 自动化 Promise-based FTP 引擎管道 ([packages/common/ftp_client.js](file:///e:/vfusion/packages/common/ftp_client.js))
* **视频网端 (Collector) 自动上传**：提交单据时，若启用了远程 FTP 通道，系统自动通过原子式 `.tmp` 上传并重命名，将打好的 Zip 数据包自动推送到指定的第三方 FTP 目录。
* **内网端 (Core) 自动轮询抓取下载与清理**：扫描循环启动时，系统自动连接指定的第三方 FTP 服务器，仅拉取符合 `pkg_prefix` 前缀的 Zip 文件下载到内网处理，并在成功入库后自动清理远程文件，防重且绝对不会干涉/污染 FTP 服务器上的无关文件。
---

## [v0.9.16] - 2026-08-11

### 🏗️ 视频网与内网前端按页面 HTML / CSS / JS 彻底模块化拆分 (Frontend Page-based Modular Split Release v0.9.16)

#### 1. 📦 视频网发布终端前端文件解耦 (`packages/collector/public`)
* **主 Shell 骨架重构 (`index.html`)**：主 HTML 文件代码行数由原本 1307 行巨型单文件成功瘦身至 150 行，负责挂载公共头尾、全局脚本与页签容器。
* **HTML 页面片段拆分 (`pages/`)**：按 Tab 页面功能拆分为 `publish.html` (采集上报)、`history.html` (已提交历史)、`builder.html` (表单设计器)、`audits.html` (审计日志)、`users.html` (用户权限) 等独立模板。
* **CSS 样式表解耦 (`css/`)**：提炼全局基础设计 Token `main.css` 以及 5 个页面的独立 CSS 样式文件（`css/pages/*.css`）。
* **JS 业务代码拆分 (`js/`)**：提炼全局入口、模板预加载器与通用控制逻辑 `main.js` 以及各页面的独立业务脚本（`js/pages/*.js`）。

#### 2. ⚡ 内网接收中台前端文件解耦 (`packages/core/public`)
* **主 Shell 骨架重构 (`index.html`)**：主 HTML 文件代码行数由原本 1734 行巨型单文件成功瘦身至 230 行。
* **全量 9 大 HTML 页面片段拆分 (`pages/`)**：按 Tab 拆分为 `events.html` (事件汇聚)、`analytics.html` (分析图表)、`builder.html` (表单设计器)、`webhooks.html` (消息分发)、`audits.html` (审计日志)、`personnel.html` (涉事人员库)、`users.html` (用户管理)、`errors.html` (死信隔离区)、`system.html` (存储健康度) 等 9 大页面模板。
* **CSS 样式表解耦 (`css/`)**：提炼全局 `main.css` 以及 9 个页面的独立 CSS 样式表（`css/pages/*.css`）。
* **JS 业务代码拆分 (`js/`)**：提炼全局入口 `main.js` 以及 9 个页面的独立业务逻辑脚本（`js/pages/*.js`）。

#### 3. 🧪 自动化服务加载与全量 48 个拆分文件 HTTP 200 校验
* **服务预加载与解耦机制**：在 `js/main.js` 中引入异步模板加载机制，应用初始化时全量预加载 `pages/*.html` 片段并绑定交互，保证无缝平滑切页体验。
* **自动化测试**：自动化测试工具验证视频网与内网两端共 48 个 HTTP 静态请求，全部 200 OK，零功能倒退。

---

## [v0.9.15] - 2026-08-11

### 📑 视频网全表格客户端分页、DOM 解耦修复与双端健康度巩固 (Video Net Pagination & DOM Repair Release v0.9.15)

#### 1. 📊 视频网采集端全量数据表格通用客户端分页 (Collector Universal Pagination Bar)
* **全量表格覆盖**：在视频网端【已提交历史存照控制台】(`historyPagination`)、【系统安全审计日志】(`auditPagination`)、【视频网用户与 RBAC 权限管理】(`usersPagination`) 与【表单设计器已生效字段列表】(`schemaPagination`) 4 大表格底部，全面集成高密度商业级 `.pagination-bar` 分页条。
* **灵活条数切换与分页控制**：统一支持 `10 条/页`、`20 条/页`、`50 条/页` 动态切换，实时显示【总记录数】与【当前页/总页数】，并根据边界自动启用/禁用【上一页】与【下一页】按钮。
* **数据切片与全局序号计算**：JavaScript 增加独立分页状态变量与缓存机制，下拉/搜索后自动重置为第 1 页并按 `(currentPage-1)*pageSize` 切片渲染，全局序号按行展示。

#### 2. 🐛 视频网端【切页签全屏空白】DOM 嵌套致命 Bug 彻底修补
* **DOM 树解耦**：排查并消除了 `tab-publish` 结尾缺失 `</div>` 导致后续页签（`tab-history`、`tab-builder`、`tab-audits`、`tab-users`）被错误嵌套为子节点的严重问题。
* **平滑切页**：补全闭合标签后，5 大页签恢复为同级 DOM 兄弟节点，任意切换页签均能正常呈现卡片与表格数据。

#### 3. ⚡ 语法错漏修补与 Favicon 204 / Inline SVG 优化
* **`handleLogin` 异步语法修补**：补全 `handleLogin` 前面的 `async function` 关键字声明，解决 Chrome 浏览器抛出 `Uncaught SyntaxError: await is only valid in async functions` 错误。
* **Favicon 204 & Inline SVG**：在 Express 服务中增加 `/favicon.ico` 的 HTTP 204 No Content 响应，并在 `<head>` 中嵌入矢量 Emoji Icon，彻底消除浏览器控制台 404 资源加载警告。

#### 4. 🐛 内网中台【死信隔离区】与【存储与健康度】顶部空白异常修补
* **容器闭合修复**：排查并消除了 `tab-users` 底部多余的 `</div>` 闭合标签（该标签此前导致 `.container` 在前 6 个页签结束时提前闭合，导致后 2 个页签渲染在容器外引发顶部巨大空白），恢复完美顶部 padding 与顶格布局。

---

## [v0.9.14] - 2026-08-11

### 📑 视频网端全表格通用客户端分页器与内网中台布局修复 (Collector All-Table Pagination & Layout Repair v0.9.14)

#### 1. 📊 视频网采集端全量 4 大表格通用客户端分页 (Collector Universal Pagination Bar)
* **全量表格覆盖**：在视频网端【已提交历史存照控制台】(`historyPagination`)、【系统安全审计日志】(`auditPagination`)、【视频网用户与 RBAC 权限管理】(`usersPagination`) 与【表单设计器已生效字段列表】(`schemaPagination`) 4 大表格底部，全面集成高密度商业级 `.pagination-bar` 分页条。
* **灵活条数切换与分页控制**：统一支持 `10 条/页`、`20 条/页`、`50 条/页` 动态切换，实时显示【总记录数】与【当前页/总页数】，并根据边界自动启用/禁用【上一页】与【下一页】按钮。
* **重构客户端切片机制**：JavaScript 分页状态与数据缓存重构，搜索/下拉过滤后自动重置为第 1 页并按 `(currentPage-1)*pageSize` 切片渲染，且按全局行标展示准确序号。

#### 2. 🧱 视频网端与内网中台 HTML DOM 结构大清洗与布局修补
* **修复视频网端 用户与 RBAC 页面截断**：修补视频网 HTML `tab-users` 误截断的左栏新建账号表单与右栏 `usersTableBody` 表格，并将 Modal Lightbox 弹窗统一下沉至 Body 根节点外层。
* **修复内网中台【死信隔离区】与【存储与健康度】顶部空白异常**：排查并消除了 `tab-users` 底部多余的 `</div>` 闭合标签（该标签此前导致 `.container` 在前 6 个页签结束时提前闭合，导致后 2 个页签渲染在容器外引发顶部巨大空白），恢复完美顶部 padding 与顶格布局。

---

## [v0.9.13] - 2026-08-11

### 📸 视频网发布端已提交历史存照控制台与本地文件凭证归档 (Collector Published History Release v0.9.13)

#### 1. 📁 视频网端【已提交历史存照】专属控制台 (Collector Published History Tab)
* **新增页签控制台**：视频网采集终端 Header 顶部新增【已提交历史存照】控制台页签。
* **全量已打包单据追溯**：可展示本终端已成功打包并投递至网闸发送目录的所有历史事件单据、关联任务名称/编号、提交时间、发生地点、涉事人员与打包状态。

#### 2. 🖼️ 本地照片凭证持久化与弹窗预览 (Local Assets & Lightbox Modal)
* **照片本地存照归档**：单据打包成功后，自动在视频网端存储附件至 `storage/collector_assets/{event_id}/`，并由 Express 静态路由 `/collector-assets/` 进行本地挂载。
* **弹窗放大与档案查看**：支持点击缩略图触发 Modal Lightbox 半透明磨砂弹窗居中放大预览，点击涉事人员查看完整详细档案。

#### 3. 📑 全表格客户端分页与下拉过滤 (History Pagination & Filter)
* **内置客户端分页条**：在历史存照表格底部集成通用 `.pagination-bar` 分页条，支持 `10/20/50` 动态条数切换。
* **按任务名称一键过滤**：支持按任务名称下拉单选与关键字实时全局搜索。

---

## [v0.9.12] - 2026-08-11

### 📑 全量数据表格通用客户端分页器与企业级看板精炼 (Full Table Client Pagination Release v0.9.12)

#### 1. 📊 全量数据表格通用客户端分页组件 (Universal Pagination Bar)
* **四大数据表格全面覆盖**：在中台【汇聚事件与数据看板】、【系统安全审计日志】、【涉事人员档案库】与【系统用户与 RBAC 管理】所有数据表格底部，全面内置高密度商业级 `.pagination-bar` 分页条。
* **灵活分页控制**：支持动态切换每页显示条数（`10 条/页`、`20 条/页`、`50 条/页`）、实时显示【总记录数】与【当前页/总页数】，并根据边界自动启用/禁用【上一页】与【下一页】按钮。

#### 2. ✂️ 数据表格布局精炼与体验升级
* **拆分任务列**：将原先【所属任务名称 / 关联编号】专栏拆分为两列独立的 `所属任务名称` 与 `关联任务编号`，列名与数据排版更加条理清晰。
* **发生地点列加宽与截断保护**：拓宽【发生地点】列展示宽度（`140px~220px`），超出自动以 `...` 截断，鼠标悬浮通过 Tooltip 展现完整地址。
* **精炼操作列**：表格最后一列【操作】隐藏冗余的 `Zip存照` 按钮（保留干练的 `详情` 按钮，存照下载可直接在详情抽屉中一键触发）。
* **移除下划线修饰**：【涉事人员】姓名链接样式移除文本下划线 (`text-decoration: none`)，展现更整洁的纯色可交互文本。

---

## [v0.9.11] - 2026-08-11

### 🎯 任务上下文管理、按任务分库分包层级归档与任务看板筛选 (Task Context Management & Hierarchical Asset Storage v0.9.11)

#### 1. 📝 视频网发布端任务上下文录入 (Task Context Inputs)
* **任务名称与关联代号录入**：事件发布表单顶部显眼新增【所属任务名称】（如 `厂区周界巡检`）与【关联任务编号】（如 `TASK_20260811_01`）录入域。
* **单据任务隔离防混淆**：所有抓拍与单据负荷强制携带任务上下文，解决多任务并行时单据与照片混淆的隐患。

#### 2. 🗂️ 内网中台物理存储与文件库层级重构 (Task-Based Asset Hierarchy)
* **物理文件库层级归档**：解包保存照片凭证时，附件存储路径重构升级为**层级化任务结构**：`storage/assets/tasks/{task_code}/{event_id}/`。
* **数据库索引扩展**：SQLite `events` 数据表全面扩展 `task_name` 与 `task_code` 数据库列与索引，保证高效并发 SQL 检索。

#### 3. 📊 内网看板【所属任务/关联编号】列与按任务筛选 (Task Dashboard & Filter)
* **数据表格任务专栏**：数据表格显眼展示【所属任务名称 / 关联编号】专栏，蓝黑双色展示任务名称与唯一任务代号。
* **按任务名称一键筛选**：看板顶部筛选栏新增【按任务名称筛选】下拉控件，动态提取并汇总中台已入库的全量任务供精准过滤。

---

## [v0.9.10] - 2026-08-11

### 🎨 版面微调、操作列靠左对齐、日志 340px 宽屏与全量两端功能对应验证 (Layout Polish & Dual-End Alignment v0.9.10)

#### 1. ⚙️ 操作列重命名与靠左对齐 (Left-aligned Operation Column)
* **表头文案精简**：数据表格最后一列表头文案由 `操作动作` 重命名为更干练的 **`操作`**。
* **左对齐一致性**：表头 `<th>` 与单元格 `<td>` 统一调整为靠左对齐 (`text-align: left`)，与整张表格其他列保持一致的对齐感受。

#### 2. 🧱 数据表格完整四周边框与紧凑列宽 (Full Cell Borders & Compact Layout)
* **四周边框绘制**：恢复并强化了表格单元格四周边框的完整显式绘制 (`border: 1px solid var(--border-color)`)，界面层次分明。
* **紧凑列宽 padding**：优化默认 padding（`0.5rem 0.65rem`），减少冗余空白间距。

#### 3. 📰 摆渡实时日志侧边栏拓宽至 340px 并开启自动换行 (340px Log Sidebar with Word Wrap)
* **宽度拓宽**：将内网中台右侧日志卡片拓宽至 **`340px`**，为长文件名与 Hash 留出充裕显示空间。
* **自动换行保护**：增加 `word-break: break-all; overflow-wrap: break-word;`，彻底解决长文件名与 Hash 字符超出截断问题。

#### 4. 🏷️ AI 算法特征列瘦身与微徽章 (Compact AI Badges)
* **微徽章排版**：AI 算法提取特征重构为极简微徽章（`HMAC防篡改`、`YOLOv8`、`FaceNet`、`AI识别`），限制最大宽度为 `140px`。
* **Tooltip 完整明细**：鼠标悬停在徽章上依然可以通过 Tooltip 展示完整解析说明。

#### 5. 🔍 视频网端与内网端 100% 功能完整度与对应性校验
* **字段全链路审计**：经对必填/选填事件发布字段、涉事人员档案提取、现场照片 Modal 预览、网闸摆渡、安全审计日志等进行全量对齐，视频网发布端与内网中台功能 **100% 完美对应**。
* **自动化测试套件**：通过 `verify_e2e.js`（端到端链路）与 `test.js`（单元测试）100% 绿灯验证。

---

## [v0.9.9] - 2026-08-11

### 💎 高密度数据表格重构、无 Body 滚动条 100vh 布局与架构精简 (Product-Grade Data Table & Zero Body Scrollbar Release v0.9.9)

#### 1. 📊 高密度产品级数据表格 (Data Table) 替代卡片
* **数据表格重构**：内网看板全面升级为高密度 Data Table 形式，罗列展示【序号】、【事件单据编号】、【提交时间】、【录入人员】、【发生地点】、【交通方式】、【涉事人员】、【现场照片】、【AI算法特征】与【操作】。
* **彻底消除水平滚动条**：重构表格弹性列宽，100% 刚好填充卡片宽度，无横向滚动条。

#### 2. 👤 涉事人员单元格精简与详细档案 Modal 弹窗
* **单元格精简**：表格【涉事人员】列仅显示姓名（如 `张三`），干净整洁。
* **详细档案 Modal 弹窗**：点击姓名触发居中 `涉事人员详细档案` 弹窗，清晰展现【姓名】、【身份证号码】与【户籍所在地】。

#### 3. 🖼️ 现场照片凭证 Modal Lightbox 弹窗预览
* **弹窗替代新标签页**：点击照片缩略图直接弹出 Modal Lightbox 半透明磨砂弹窗居中放大预览，告别浏览器新标签页跳转。
* **快捷关闭**：支持点击右上角 `✕` 按钮、点击遮罩背景或按键盘 `Esc` 键一键关闭。

#### 4. 🧹 威胁等级全量清理与 100% 零 Emoji 纯矢量 UI 规范
* **威胁等级清理**：全量移除 `threat_level` 徽章、筛选框与饼图，替换为交通方式分布统计。
* **零 Emoji 纯矢量 UI**：全量清理前端及后端日志中的 Emoji 表情，严格遵循纯矢量 SVG 图标与商业干练文本规范。

#### 5. 📐 页面无 Body 滚动条与 100vh Viewport 视口重构
* **Body 零滚动条 (`overflow: hidden`)**：双端页面 `html, body` 统一设为 `height: 100vh; overflow: hidden;`，页面整体完美贴合屏幕视口。

#### 6. ✂️ 移除多业务应用租户选择器
* 精简移除前端 header 中的应用租户切换下拉框，系统默认统一标准化为通用安防交换通道 (`sys_gate_security`)。

---

## [v0.9.8] - 2026-08-11

### 👤 默认事件字段重构、涉事人员历史档案自动归档与一键快捷提取 (Personnel Archive & Default Schema v0.9.8)

#### 1. 📋 默认事件发布字段全量重构 (Default Event Fields)
* **默认 Schema 结构更新**：配置为【事件时间 (必填且预填当前时间)】、【交通方式 (必填下拉单选)】、【地点 (必填)】、【经度】、【纬度】、【事件描述】、【涉事人员姓名】、【涉事人员身份证号码】、【涉事人员户籍地址】。

#### 2. 👤 涉事人员历史档案一键提取与自动归档 (Personnel Archive & Quick Auto-Fill)
* **高亮显眼下拉组件**：视频网发布端显眼渲染 `涉事人员历史档案 (点击选择一键填入)` 蓝色高亮控件。
* **一键自动填入**：选择已有历史人员后，自动一次性填充【姓名】、【身份证号码】与【户籍地址】。
* **自动归档与提交即时刷新**：提交新涉事人员单据后，系统自动归档并实时刷新下拉选项，无需手动刷新网页即可在下一次录入时直接提取。

#### 3. 🏢 内网中台跨网单向摆渡自动归档与档案库控制台 (Core Personnel Auto-Sync & Tab)
* **单向摆渡自动提取归档**：中台解包校验单据时，自动解析提取涉事人员信息并归档入库。
* **中台档案库控制台**：新增 `涉事人员档案库` 专属页签，展示人员历史迹线、最新出现时间与关联单据编号。

#### 4. 🛠️ 输入框交互重构与失焦防擦除保护 (Form Input Focus & Value Preservation)
* 移除破坏性定时轮询 DOM 清空重绘，彻底解决视频网打字失焦与无法输入问题。
* 增加输入框已填字符暂存保护机制，确保界面交互流畅稳定。

---

## [v0.9.7] - 2026-08-11

### 👑 商业级全量升级：SQLite 数据库、ONNX AI 管道、TLS 证书与开机自启服务 (Commercial Production Release v0.9.7)
* **SQLite 嵌入式数据库**：引入原生 `sqlite3` 驱动 (`db_sqlite.js`)，支持 `vfusion_core.db` 与 `vfusion_collector.db` 高并发高效 SQL 检索。
* **本地 AI 推理管道**：集成 YOLOv8 车牌识别、FaceNet 128维人像特征码比对与 HelmetNet 安全帽未佩戴告警。
* **开机自启系统服务安装包**：提供 Windows 服务注册工具 `scripts/install_service.bat` 与 Linux `systemd` 配置文件。
* **TLS / HTTPS 证书自动管理**：内置 2048 位 RSA 密钥对与 TLS 自签名证书生成模块 (`ssl_cert.js`)。

---

## [v0.9.6] - 2026-08-10

### 🚀 企业产品级全量优化与微交互提升 (Product-Grade Fine-tuning v0.9.6)
* 全局 `Esc` 快捷键抽屉关闭、网闸状态脉冲灯 (`pulse-dot`) 联动、签名解包 API (`verifyInfoSignature`) 补全。

---

## [v0.9.5] - 2026-08-10

### 📹 视频网发布端 RBAC 权限管理与系统安全审计日志 (Collector RBAC & Audit Logs v0.9.5)
* 视频网物理隔离端独立鉴权 API（`/api/auth/login` 与 `/api/users`）、三级角色隔离（`admin`, `operator`, `auditor`）及独立系统安全审计日志。

---

## [v0.9.4] - 2026-08-10

### 🛡️ 视汇 Brand Logo Icon 视觉品牌升级 (Cyber Optical Shield Icon v0.9.4)
* **专属“天眼+安防盾”视觉 Logo**替换全量 Header 与登录 Portal 图标。

---

## [v0.9.3] - 2026-08-10

### 🇨🇳 系统安全审计日志全量中文化 (Full Chinese Audit Logs v0.9.3)
* 20 项操作类别与 4 项状态级别全量中文化显示，CSV 导出联动中文化。

---

## [v0.9.2] - 2026-08-10

### 📐 全量页面左右双栏布局重构与无滚动条大屏全览 (2-Column Split Layout v0.9.2)
* 视频网发布端与内网中台全量重构为左右双栏，消除页面级 Body 滚动条。

---

## [v0.9.1] - 2026-08-10

### ✨ UI/UX 全量产品级打磨与 100% 零 Emoji 纯矢量化 (UI Polish & Zero-Emoji v0.9.1)
* 100% 零 Emoji 纯矢量替换，主按钮微渐变设计系统。

---

## [v0.9.0] - 2026-08-10

### 🏆 全量功能打磨与 v0.9.0 终极完整版 (Full Release Candidate v0.9.0)
* AI 智能分析管道、24h 趋势折线图与一键部署脚本 (`start_all.bat` / `start_all.sh`)。
